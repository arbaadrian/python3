#!/usr/bin/env bash

RUNNING_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )";

docker run \
  --rm \
  -it \
  --name rpm-demo \
  -v ${RUNNING_DIR}/app:/opt/pyth/app \
  demo-centos-7-py-36:1.0.0 \
  bash

